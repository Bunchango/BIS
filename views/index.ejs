<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home page</title>
</head>

<body>
    <% if (user) { %>
        <h1> HELLO <%= user.username %>
        </h1>
        <img src="<%= user.profilePicture.url %>" />
        <a href="/checkin/logout">Log out</a>
        <% } else { %>
            <h1> HELLO Guest</h1>
            <% } %>
                <h1> HELLO WORLD</h1>
                <!--Testing search bar-->
                <%- include('partials/search.ejs') %>
</body>

</html>
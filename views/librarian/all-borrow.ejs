<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" href="images/vxnhe-icon.png" type="image/x-icon">
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles/librarian-role.css">
  <link rel="stylesheet" href="/styles/footer.css"/>
  <link rel="stylesheet" href="/styles/customer.css">
  <link rel="stylesheet" href="https://unicons.iconscout.com/release/v4.0.0/css/line.css">
  <title>Document</title>
</head>
<body>
<%- include('./partials-librarian/nav-librarian.ejs')%>
<section class="search-section">
  <label for="search">Search for Borrows</label>
  <input type="search" id="search" data-borrow-search>
</section>

<section id="borrow-section" class="p-16 pb-0">
  <h2 class="text-2xl font-bold mb-4" id="sect-header">Borrows</h2>
  <% if (borrows && borrows.length > 0) { %>
    
    <ul class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-3 xl:grid-cols-3 gap-10" id="pagination-list" data-borrows-container>
        <!-- <% borrows.forEach(borrow => { %>
          
            <a href="/librarian/borrow/<%= borrow._id %>" class="p-4 rounded-md shadow-md" id="pagination-item">
              <div class="rounded-full overflow-hidden w-40 h-40 mx-auto mb-4">
                <img src="/<%= borrow.reader.profilePicture %>" alt="Image 1" class="w-full h-full object-cover">
              </div>
              <h3 class="text-xl font-bold mb-2 text-center" data-header><%= borrow.reader.username %></h3>
              <div data-body>
                <p class="text-center">Email: <%= borrow.reader.gmail %></p>
                <p class="text-center">Created On: <%= borrow.createdOn %></p>
              </div>
            </a>
        <% }); %> -->
    </ul>
  <% } %>
  <template data-borrows-template>
    <a class="p-4 rounded-md shadow-md" id="pagination-item" >
      <div class="rounded-full overflow-hidden w-40 h-40 mx-auto mb-4" data-img>
        <img src="" alt="" class="w-full h-full object-cover">
      </div>
      <h3 class="text-xl font-bold mb-2 text-center" data-header></h3>
      <div data-body class="text-center">
      </div>
    </a>
  </template>
   
</section>
<script src="/js/librarian-role.js"></script>
<script src="/js/librarian-search.js"></script>
<%- include("../partials_readers/footer.ejs") %>
</body>
</html>